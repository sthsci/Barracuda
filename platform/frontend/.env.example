# mock keeps all examples and guest analyses in memory.
# Set to http when the platform API contract is available.
NEXT_PUBLIC_API_MODE=mock
# Browser requests stay same-origin. Next proxies /api/v1 to the internal API.
NEXT_PUBLIC_API_URL=/api/v1
BARRACUDA_API_INTERNAL_URL=http://api:8000
