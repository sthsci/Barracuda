# Vendored inference dependency

`bayesorca-0.1.0-py3-none-any.whl` was built and validated from the repository's
`pypackage` branch. It is included so this standalone web branch can build from
a private repository without embedding GitHub credentials.

After `bayesorca` is published on PyPI, replace the local wheel requirement with
an exact PyPI version and remove the wheel from this directory.
