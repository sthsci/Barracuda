"""Simulation and donor ignorant inference for ordered contact trajectories."""

try:
    from ._core.trajectory import (
        BACKEND_TO_PUBLIC_PARAMETER,
        CANONICAL_COLUMNS,
        DEFAULT_CONDITION,
        PUBLIC_PARAMETERS,
        PUBLIC_TO_BACKEND_PARAMETER,
        TRAJECTORY_MODEL_SPECS,
        TrajectoryModelSpec,
        TrajectoryResult,
        TrajectorySettings,
        TrajectorySimulationSpec,
        build_trajectory_archive,
        expanded_trajectory_frame,
        normalize_trajectory_frame,
        read_trajectory_csv,
        run_trajectory_conditions,
        simulate_trajectory_frame,
        trajectory_evidence_frame,
        trajectory_posterior_draws,
        trajectory_summary_frame,
        truth_model_key,
        validate_trajectory_frame,
    )
except ModuleNotFoundError as exc:
    if not exc.name or not exc.name.startswith("barracuda._core"):
        raise
    from webapp.core.trajectory import (
        BACKEND_TO_PUBLIC_PARAMETER,
        CANONICAL_COLUMNS,
        DEFAULT_CONDITION,
        PUBLIC_PARAMETERS,
        PUBLIC_TO_BACKEND_PARAMETER,
        TRAJECTORY_MODEL_SPECS,
        TrajectoryModelSpec,
        TrajectoryResult,
        TrajectorySettings,
        TrajectorySimulationSpec,
        build_trajectory_archive,
        expanded_trajectory_frame,
        normalize_trajectory_frame,
        read_trajectory_csv,
        run_trajectory_conditions,
        simulate_trajectory_frame,
        trajectory_evidence_frame,
        trajectory_posterior_draws,
        trajectory_summary_frame,
        truth_model_key,
        validate_trajectory_frame,
    )

__all__ = [
    "BACKEND_TO_PUBLIC_PARAMETER",
    "CANONICAL_COLUMNS",
    "DEFAULT_CONDITION",
    "PUBLIC_PARAMETERS",
    "PUBLIC_TO_BACKEND_PARAMETER",
    "TRAJECTORY_MODEL_SPECS",
    "TrajectoryModelSpec",
    "TrajectoryResult",
    "TrajectorySettings",
    "TrajectorySimulationSpec",
    "build_trajectory_archive",
    "expanded_trajectory_frame",
    "normalize_trajectory_frame",
    "read_trajectory_csv",
    "run_trajectory_conditions",
    "simulate_trajectory_frame",
    "trajectory_evidence_frame",
    "trajectory_posterior_draws",
    "trajectory_summary_frame",
    "truth_model_key",
    "validate_trajectory_frame",
]
